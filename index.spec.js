describe('getPosition Map', ()=>{
    it('should exist a variable named "getPosition"',()=>{
        expect('getPosition').not.toBe(undefined)
    })
    it('should be a function', ()=>{
        expect(typeof getPosition).toEqual('function')
    })
    //siento mucho no poder terminar e tenido problemas con el proyecto y e tenido que rehacer todo en 2 dias//
})